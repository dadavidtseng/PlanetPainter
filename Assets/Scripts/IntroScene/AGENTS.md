[Root](../../../AGENTS.md) > `Assets/Scripts/IntroScene/`

# IntroScene

`UI_Intro` plays two sequential `VideoPlayer` clips and transitions through `ISceneService` after the second clip reaches its endpoint. It is a small scene-specific UI assembly depending on SceneTransition, Extenject, and Unity video APIs. No tests discovered.
