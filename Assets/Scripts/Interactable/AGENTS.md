[Root](../../../AGENTS.md) > `Assets/Scripts/Interactable/`

# Interactable

Defines `BaseInteractableView`, the shared MonoBehaviour contract for `CanInteract`, `IsInteracted`, sprite rendering, and virtual `Interact()`. `DoorView` and `SwitchView` extend it. This assembly has no service or installer of its own and no tests discovered.
